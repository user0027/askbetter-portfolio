# askbetter-portfolio 작업 규칙

이 파일은 `.assetsignore` 에 들어 있어 배포되지 않는다.

## 소개 영역의 회사 로고는 유지한다

소개(자기소개·경력) 영역에는 회사 로고(`assets/logos/fromthered.png`·`douzone.png`·
`estsecurity.png`)를 회사명과 함께 노출한다. 이력서 정보로 필요하다는 것이 사용자 요청이다.

- 구조를 개편하더라도 로고를 빼지 않는다. 배치가 바뀌면 새 구조 안에 다시 넣는다.
- 2026-09-25 히어로 개편 커밋(c466f34)에서 경력 섹션을 질문형 카드로 바꾸며 로고 세 개가
  빠졌고, 사용자가 이미 요청했던 사항이라고 다시 지적했다. 이 요청이 메모리에만 있어서
  그 세션에서 떠오르지 않은 것이 원인이다.

## 본문 틀의 좌우 선은 헤더와 같다

모든 페이지의 본문 틀(`.page`·`.wrap`)은 헤더 로고 왼쪽 선과 메뉴 오른쪽 선에 맞춘다.
값은 `assets/nav.css` 의 `--page-max`·`--page-gut` 한 곳에 있고, 페이지마다 폭을 새로 정하지 않는다.
긴 글줄은 틀을 좁히지 말고 `p`·`li` 에 `--read-max` 를 걸어 끊는다.

- 레이아웃을 건드렸으면 보고하기 전에 `python3 scripts/check-margins.py` 를 돌린다.
  세 페이지를 폭 1440·1280·900·500 에서 재고, 어긋나면 1로 끝난다.
- 2026-09-26 에 같은 지적을 두 번 받았다. 인터뷰 본문이 가운데 820px 단이라 먼저 왼쪽 선이,
  고친 뒤에는 오른쪽 선이 헤더와 어긋났다. 한쪽 선만 맞추고 반대쪽을 재지 않은 것이 원인이다.
